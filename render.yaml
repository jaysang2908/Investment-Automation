services:
  - type: web
    name: investment-automation
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: FMP_API_KEY
        sync: false
      - key: APP_PASSWORD
        sync: false
      - key: GEMINI_KEY
        sync: false
      - key: GITHUB_TOKEN
        sync: false
      - key: GITHUB_REPO
        sync: false

  - type: cron
    name: daily-news-refresh
    runtime: python
    buildCommand: pip install -r requirements.txt
    schedule: "0 6 * * *"
    startCommand: python daily_news.py
    envVars:
      - key: FMP_API_KEY
        sync: false
      - key: GITHUB_TOKEN
        sync: false
      - key: GITHUB_REPO
        sync: false

  - type: cron
    name: daily-long-term-bets
    runtime: python
    buildCommand: pip install -r requirements.txt
    schedule: "0 2 * * *"
    startCommand: python daily_long_term_bets.py
    envVars:
      - key: APP_PASSWORD
        sync: false
      - key: RENDER_INTERNAL_URL
        value: https://investment-automation.onrender.com
